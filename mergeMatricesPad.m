function merged = mergeMatricesPad(padValue, varargin)
% mergeMatricesPad  Merge multiple matrices of different sizes into one
%   merged = mergeMatricesPad(padValue, M1, M2, M3, ...)
%   padValue: value used to fill missing elements (e.g., NaN or 0)
%   M1, M2, ...: matrices to merge vertically

merged = mergeMatricesPad(0, Age, day1toDay2, day2toDay3, Day1, Day2, Day3, femaleGroupIsoMean, femaleGroupIsoMean, Gender, maleGroupIsoMean, maleGroupIsoMean, normDay1mean, normDay2mean, normDay3mean, subjectID, SubjectID, Weight)

end